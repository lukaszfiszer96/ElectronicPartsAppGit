@model IEnumerable<string>


@foreach (var dane in Model)
{
    @Html.RouteLink(dane,new {

    controller = "ElectronicPart",
    action = "List",
    category = dane



}, new { @class = "btn btn-default btn-lg" + (dane == ViewBag.WybranaKat ? "btn btn-primary btn-lg" : "") })
}
